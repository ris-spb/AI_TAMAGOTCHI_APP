# KTX2 — EXTERNAL PRODUCTION / VALIDATION REQUIRED
**Stage:** 17
**Status:** `NOT PRODUCED`

KTX2/Basis compatibility was not physically validated because Stage-16 device tests were waived.
Do not invent `.ktx2` files.

The runtime may initially ship compatible non-KTX2 texture sources until the external DCC/frontend pipeline creates and validates compressed textures.
