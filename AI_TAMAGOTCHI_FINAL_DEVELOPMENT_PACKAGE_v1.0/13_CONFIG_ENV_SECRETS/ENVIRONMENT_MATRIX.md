# ENVIRONMENT MATRIX

`local`, `test`, `staging`, `production` are `SAFE_ENGINEERING_DEFAULT` environment classes, not evidence that corporate infrastructure already exists.

Production rejects mock Personnel/LLM/STT, any HARD_DISABLED_MVP flag enabled, external notifications enabled, missing required secrets/config and unverified claims of final production 3D.
